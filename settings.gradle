include  ':EventBusLibrary'

if (Boolean.parseBoolean(Main_APP)) {
    include ':app'
}
if (Boolean.parseBoolean(PLUGIN_A)) {
    include ':plugin_a'
}

if (Boolean.parseBoolean(PLUGIN_B)) {
    include ':plugin_b'
}